# `metier/` — services métiers

Vide pour l'instant. C'est ici que vivra le premier service métier réel,
sur le même modèle que `infra/`/`sc/` (1 dossier = 1 service à l'intérieur),
avec son allocation dans le bloc réseau `10.2.0.0/16` (cf. `README.md` à la
racine, « Plan d'adressage réseau »).
